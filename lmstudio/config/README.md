# Config directory (legacy)

Canonical configs:

- MCP JSON: [`../mcp/`](../mcp/)
- System prompt: [`../prompts/system-prompt.md`](../prompts/system-prompt.md)

Installers read from `mcp/mcp.json` directly. Files here are kept for backward compatibility.
